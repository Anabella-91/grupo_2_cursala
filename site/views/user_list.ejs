<body>
    <h1>Usuarios Registrados</h1>
    <ul>
        <% for(let i=0; i< user.length; i++){ %>
            <li>
                <%= user[i].name %> 
            </li>
        <% } %>
    </ul>
</body>